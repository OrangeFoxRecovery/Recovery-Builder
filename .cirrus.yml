env:
    # OrangeFox Sync Repository
    FOX_SYNC: https://gitlab.com/OrangeFox/sync.git

    # Device
    FOX_BRANCH: fox_9.0
    DT_LINK: https://github.com/Azure-007/android_device_xiaomi_raphael-ofox.git -b a11fw
    DEVICE: raphael
    OEM: xiaomi
    TARGET: recoveryimage

    DT_PATH: device/$OEM/$DEVICE
    OUTPUT: OrangeFox*.zip

    # Extra Command
    EXTRA_CMD: git clone https://github.com/OrangeFoxRecovery/Avatar.git misc

    # Not Recommended to Change
    SYNC_PATH: $HOME/work # Full (absolute) path. Use "$HOME" instead of "~".
    USE_CCACHE: 1
    CCACHE_SIZE: 50G
    CCACHE_DIR: $HOME/work/.ccache
    J_VAL: 16

    # Cirrus Specific Vars (Do *NOT* Change)
    CIRRUS_SHELL: bash

    # Telegram
    TG_CHAT_ID: ENCRYPTED[303704c445ef6e57ed782c584c4826bf1bb842cc66c148d6813d964d2fb7206b40ad13bb7b0becf61a604ea135877215]
    TG_TOKEN: ENCRYPTED[3b2ec5e9fb919ba546c5b471727accf908d80b641f7125c1d65931fa83dda74dc66b07836557bdc8d1c541592c91d201]

task:
  name: "Recovery Build"
  trigger_type: manual
  timeout_in: 120m
  container:
      image: ghcr.io/sushrut1101/docker:latest
      cpu: 8
      memory: 32G

  Sync_script:
      - bash sync.sh

  Build_script:
      - bash build.sh

  Upload_script:
      - export TIMEOUT=43200 # Link timeout (in minutes)
      - bash upload.sh
